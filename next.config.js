/** @type {import('next').NextConfig} */
const nextConfig = {
  // Use standard deployment with ISR for better SEO flexibility
  // Can switch to static export later if needed
};

module.exports = nextConfig;
